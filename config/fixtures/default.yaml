sylius_fixtures:
    suites:
        default:
            listeners:
                orm_purger: ~
                logger: ~
            fixtures:
                admin_user:
                    name: "admin_user"
                    options:
                        custom:
                            -
                                email: "admin@example.com"
                                username: "admin"
                                password: "adminpass"
                                enabled: true
                                first_name: "John"
                                last_name: "Doe"